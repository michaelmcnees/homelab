apiVersion: v1
kind: Service
metadata:
  name: paperless-gpt
  namespace: apps
  labels:
    app.kubernetes.io/name: paperless-gpt
spec:
  selector:
    app.kubernetes.io/name: paperless-gpt
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
