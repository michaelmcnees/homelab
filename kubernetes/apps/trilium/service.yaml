apiVersion: v1
kind: Service
metadata:
  name: trilium
  namespace: apps
  labels:
    app.kubernetes.io/name: trilium
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trilium
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
