apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-config
  namespace: apps
data:
  config.yaml: |
    model:
      provider: openai-codex
      default: gpt-5.5
    mcp_servers:
      outline:
        url: https://docs.mcnees.me/mcp
        auth: oauth
        enabled: true
