apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: adguard-k8s
  namespace: apps
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`adguard-k8s.home.mcnees.me`)
      kind: Rule
      services:
        - name: adguard
          port: 80
  tls:
    secretName: legacy-home-wildcard-tls
