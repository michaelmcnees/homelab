apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: hdf
  namespace: flux-system
spec:
  interval: 10m
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/hdf
  prune: true
  wait: true
  dependsOn:
    - name: infrastructure-configs
    - name: databases
    - name: storage
  decryption:
    provider: sops
    secretRef:
      name: sops-age
