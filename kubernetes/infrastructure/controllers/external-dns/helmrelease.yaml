apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns
  namespace: infrastructure
spec:
  driftDetection:
    mode: enabled
  interval: 30m
  dependsOn:
    - name: traefik
      namespace: infrastructure
  chart:
    spec:
      chart: external-dns
      version: "1.21.1"
      sourceRef:
        kind: HelmRepository
        name: external-dns
        namespace: flux-system
      interval: 12h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    domainFilters:
      - mcnees.me
      - dvflw.co
    env:
      - name: CF_API_TOKEN
        valueFrom:
          secretKeyRef:
            name: cloudflare-api-token
            key: api-token
    excludeDomains:
      - home.mcnees.me
      - dev.home.mcnees.me
    extraArgs:
      - --cloudflare-proxied
      - --cloudflare-dns-records-per-page=5000
      - --default-targets=104.14.105.18
    interval: 5m
    policy: upsert-only
    provider:
      name: cloudflare
    registry: txt
    sources:
      - crd
      - traefik-proxy
    txtOwnerId: homelab
    txtPrefix: external-dns-
    resources:
      requests:
        cpu: 25m
        memory: 64Mi
      limits:
        memory: 128Mi
