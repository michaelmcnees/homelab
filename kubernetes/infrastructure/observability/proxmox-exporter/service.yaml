apiVersion: v1
kind: Service
metadata:
  name: proxmox-exporter
  namespace: observability
  labels:
    app.kubernetes.io/name: proxmox-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proxmox-exporter
  ports:
    - name: http
      port: 9221
      targetPort: http
      protocol: TCP
