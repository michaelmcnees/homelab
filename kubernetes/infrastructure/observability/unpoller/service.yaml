apiVersion: v1
kind: Service
metadata:
  name: unpoller
  namespace: observability
  labels:
    app.kubernetes.io/name: unpoller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: unpoller
  ports:
    - name: http
      port: 9130
      targetPort: http
      protocol: TCP
